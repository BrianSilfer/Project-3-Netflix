function init() {
  //pulling movies data from
  d3.json("http://127.0.0.1:5000/movies").then((titles_data) => {
    //check that data loaded
    console.log(titles_data)
    
    for 
});
  }
    
init();